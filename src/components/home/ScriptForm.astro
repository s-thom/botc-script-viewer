---

---

<form
  id="script-form"
  method="POST"
  action="/go"
  enctype="multipart/form-data"
  class="form"
>
  <h2><label for="script">Paste script JSON or URL.</label></h2>

  <textarea class="textarea" name="script" id="script"></textarea>

  <button type="submit" class="button" data-umami-event="script-submit">
    View script
  </button>
</form>

<style>
  .form {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    width: 100%;
  }

  .textarea {
    background-color: var(--color-control-background);
    border: 2px solid var(--color-control-border);
    color: var(--color-text);
    padding: 0.5rem;
    border-radius: 15px;
    width: 100%;
    resize: vertical;

    &:disabled {
      filter: grayscale();
    }

    &:hover,
    &:focus-within {
      background-color: var(--color-control-background-hover);
      border-color: var(--color-control-border-hover);
    }

    &:active {
      background-color: var(--color-control-background-active);
      border-color: var(--color-control-border-active);
    }
  }
</style>
